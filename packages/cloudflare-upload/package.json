{
  "name": "@postplan/cloudflare-upload",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "cf:types": "wrangler types env.d.ts",
    "typecheck": "bun run cf:types && tsc --noEmit",
    "cf:build": "wrangler deploy --dry-run --outdir dist",
    "upload:test": "node test/acceptance.mjs"
  },
  "dependencies": {
    "@orpc/contract": "catalog:",
    "@orpc/openapi": "catalog:",
    "@orpc/server": "catalog:",
    "@postplan/api": "workspace:*",
    "@postplan/store": "workspace:*",
    "hono": "4.13.8",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@postplan/html-validator": "workspace:*",
    "@types/node": "catalog:",
    "esbuild": "0.28.1",
    "miniflare": "5.20260911.1-alpha",
    "wrangler": "4.131.2"
  }
}
